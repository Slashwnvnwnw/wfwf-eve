| 序号  | 接口                                                                 | 修改类型           | 入参                                                            | 出参                           | 逻辑                                                                                                                                                                           |
|:--- |:------------------------------------------------------------------ |:-------------- |:------------------------------------------------------------- |:---------------------------- |:---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 采样待办的指标结果编辑服务整改<br>`qua/web/rest/ebg/update-todo-qua-indicator`    | 接口修订-一般        | 1. fileId 文件标识删除<br>2. 新增一个 `List<String> fileIds`<br>3. 是否涉及 | 不变                           | 1. 原仅支持一个文件，当前支持最多10个文件。<br>2. 若不涉及，则考核责任人非必填；若涉及，则考核责任人必填<br>3. `t_ebg_samp_task_indicator_result` 指标分类结果表新增三个字段：`is_cse`、`is_ccr`、`is_pse`<br>4. 根据界面上选择的是否涉及进行对应字段值的填充设置。 |
| 2   | 采样待办的指标结果查询服务整改<br>`qua/web/rest/ebg/samp-task/indicator/query`    | 接口修订-简单        | 不变                                                            | 补充 cse是否涉及、ccr是否涉及、pse是否涉及响应 |                                                                                                                                                                              |
| 3   | 采样待办详情的附件列表查询服务整改<br>`qua/web/rest/ebg/samp-task/attachment/query` | 接口修订-简单        | 不变                                                            | 新增是否可以删除标记                   | 1. 模块管理员/系统管理员可删除<br>2. 若附件的上传环节与当前问题的所处环节一致，且登录人为上传人，则也可以删除。                                                                                                                |
| 4   | 采样待办详情的附件删除服务<br>`qua/web/rest/ebg/samp-task/attachment/delete`    | 新增/修改/删除类接口-简单 | 文件标识 (fileId)、待办标识 (todoId)                                   | 成功/失败                        | 1. 模块管理员/系统管理员可删除<br>2. 若附件的上传环节与当前问题的所处环节一致，且登录人为上传人，则也可以删除。<br>3. 更新：`t_ebg_samp_task_todo_attachment` 的文件状态为已删除。<br>4. 需要删除edm存储的信息。                                      |
