# 02-接口2-指标结果查询

> 本文件包含以下源码（按包结构整理）

---

```
=== service/src/main/java/com/huawei/qua/ebg/bo/samp/EbgSampTaskCategoryBo.java ===
```

```java
/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2026-2026. All rights reserved.
 */

package com.huawei.qua.ebg.bo.samp;

import com.fasterxml.jackson.annotation.JsonFormat;

import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;

import java.util.Date;

/**
 * 采样任务待办指标结果表 实体类
 *
 * @author gwx1503283
 * @since 2026-05-20
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
public class EbgSampTaskCategoryBo {
    /**
     * 主键
     */
    private Long id;

    /**
     * 业务类型，eCare-问题单质检、eCareMajor-重大问题质检
     */
    private String busiType;

    /**
     * 质检类型，问题单质检：CHECK_ITEM-质检项/MONITORING_ITEM-监控项；重大问题质检：INCIDENT_ITEM-事故/CRITICAL_ITEM-严重问题/ME_ITEM-管理升级
     */
    private String checkType;

    /**
     * 问题单号
     */
    private String srNum;

    /**
     * 采样任务待办id
     */
    private Long todoId;

    /**
     * 指标的考核责任人（CSE）,初始对应问题单owner
     */
    private String cseAssessor;

    /**
     * 指标的考核责任人（CCR）
     */
    private String ccrAssessor;

    /**
     * 指标的考核责任人（PSE）
     */
    private String pseAssessor;

    /**
     * cse是否涉及，0-涉及，1-不涉及
     */
    private String isCse;

    /**
     * ccr是否涉及，0-涉及，1-不涉及
     */
    private String isCcr;

    /**
     * pse是否涉及，0-涉及，1-不涉及
     */
    private String isPse;

    /**
     * 待办表的考核责任人（CSE）,初始对应问题单owner
     */
    private String todoCseAssessor;

    /**
     * 待办表的考核责任人（CCR）
     */
    private String todoCcrAssessor;

    /**
     * 待办表的考核责任人（PSE）
     */
    private String todoPseAssessor;

    /**
     * 初审备注
     */
    private String firstReviewRemark;

    /**
     * 申诉原因
     */
    private String appealReason;

    /**
     * 复审备注
     */
    private String finalReviewRemark;

    /**
     * 质量指标
     */
    private String indicator;

    /**
     * 指标分类
     */
    private String ecareRuleCategory;

    /**
     * 规则名称
     */
    private String ruleName;

    /**
     * 质检结果，0-通过、1-不通过
     */
    private String quantityResult;

    /**
     * 是否申诉：0-申诉，1-不申诉
     */
    private String isAppealed;

    /**
     * 是否质检，0-已质检，1-未质检
     */
    private String isCheck;

    /**
     * 申诉确认，0-已确认，1-未确认，质检指标通过的默认申诉已经确认
     */
    private String appealConfirm;

    /**
     * 质检结果，0-通过、1-不通过
     */
    private String result;

    /**
     * 审核不通过原因
     */
    private String rejectionReason;

    /**
     * 审核不通过关键字
     */
    private String rejectionKeyword;

    /**
     * 创建人（用户名或ID）
     */
    private String createUser;

    /**
     * 创建时间（自动记录）
     */
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss", timezone = "GMT+8")
    private Date createTime;
}
```

---

```
=== service/src/main/java/com/huawei/qua/ebg/outer/vo/task/EbgSampTaskResultVo.java ===
```

```java
/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2026-2026. All rights reserved.
 */

package com.huawei.qua.ebg.outer.vo.task;

import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;

import java.util.List;

/**
 * 采样任务待办指标结果表 实体类
 *
 * @author gwx1503283
 * @since 2026-05-20
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
public class EbgSampTaskResultVo {
    /**
     * 考核责任人（CSE）,初始对应问题单owner
     */
    private List<String> cseAssessor;

    /**
     * 考核责任人（CCR）
     */
    private List<String> ccrAssessor;

    /**
     * 考核责任人（PSE）
     */
    private List<String> pseAssessor;

    /**
     * 初审备注
     */
    private String firstReviewRemark;

    /**
     * 申诉原因
     */
    private String appealReason;

    /**
     * 复审备注
     */
    private String finalReviewRemark;

    /**
     * 质检指标
     */
    private String indicator;

    /**
     * 质检指标名称
     */
    private String indicatorName;

    /**
     * 质检指标结果
     */
    private String quantityResult;

    /**
     * 被质检对象
     */
    private String sample;

    /**
     * 是否申诉：0-申诉，1-不申诉
     */
    private String isAppealed;

    /**
     * 是否质检，0-已质检，1-未质检
     */
    private String isCheck;

    /**
     * 申诉确认，0-已确认，1-未确认，质检指标通过的默认申诉已经确认
     */
    private String appealConfirm;

    /**
     * 指标细项列表
     */
    private List<EbgSampTaskCategoryResultVo> category;

    /**
     * cse是否涉及，0-涉及，1-不涉及
     */
    private String isCse;

    /**
     * ccr是否涉及，0-涉及，1-不涉及
     */
    private String isCcr;

    /**
     * pse是否涉及，0-涉及，1-不涉及
     */
    private String isPse;
}
```

---

```
=== service/src/main/resources/mapper/ebg/EbgSampTaskCategoryResultMapper.xml ===
```

```java
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.huawei.qua.ebg.dao.samp.EbgSampTaskCategoryResultDao">

    <!-- 通用查询字段 -->
    <delete id="deleteTodoTaskRuleResult" parameterType="java.lang.Long">
        DELETE
        FROM t_ebg_samp_task_category_result
        WHERE id = #{id}
    </delete>

    <!-- ResultMap 映射 -->
    <insert id="batchAdd" keyProperty="id" parameterType="java.util.List" useGeneratedKeys="true">
        INSERT INTO t_ebg_samp_task_category_result (
        busi_type,
        check_type,
        todo_id,
        indicator,
        ecare_rule_category,
        rule_name,
        quantity_result,
        rejection_reason,
        rejection_keyword,
        create_user,
        create_time
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.busiType,jdbcType=VARCHAR},
            #{item.checkType,jdbcType=VARCHAR},
            #{item.todoId,jdbcType=VARCHAR},
            #{item.indicator,jdbcType=VARCHAR},
            #{item.ecareRuleCategory,jdbcType=VARCHAR},
            #{item.ruleName,jdbcType=VARCHAR},
            #{item.quantityResult,jdbcType=VARCHAR},
            #{item.rejectionReason,jdbcType=VARCHAR},
            #{item.rejectionKeyword,jdbcType=VARCHAR},
            #{item.createUser,jdbcType=VARCHAR},
            #{item.createTime,jdbcType=TIMESTAMP})
        </foreach>
    </insert>

    <!-- 新增 -->
    <insert id="insertTodoRuleTaskResult"
        keyColumn="id" keyProperty="id" parameterType="com.huawei.qua.ebg.bo.task.TodoRuleTaskResultReqBo" useGeneratedKeys="true">
        INSERT INTO t_ebg_samp_task_category_result (
            busi_type,
            check_type,
            todo_id,
            indicator,
            ecare_rule_category,
            rule_name,
            quantity_result,
            rejection_reason,
            rejection_keyword,
            create_user,
            create_time
        ) VALUES (
                     #{busiType},
                     #{checkType},
                     #{todoId},
                     #{indicator},
                     #{ecareRuleCategory},
                     #{ruleName},
                     #{quantityResult},
                     #{rejectionReason},
                     #{rejectionKeyword},
                     #{createUser},
                     now()
                 )
    </insert>

    <!-- 插入记录 -->
    <resultMap id="EbgSampTaskResultBoResultMap" type="com.huawei.qua.ebg.bo.samp.EbgSampTaskCategoryResultBo">
        <id column="id" property="id" />
        <result column="busi_type" property="busiType" />
        <result column="check_type" property="checkType" />
        <result column="todo_id" property="todoId" />
        <result column="indicator" property="indicator" />
        <result column="ecare_rule_category" property="ecareRuleCategory" />
        <result column="rule_name" property="ruleName" />
        <result column="quantity_result" property="quantityResult" />
        <result column="rejection_reason" property="rejectionReason" />
        <result column="rejection_keyword" property="rejectionKeyword" />
        <result column="create_user" property="createUser" />
        <result column="create_time" property="createTime" />
    </resultMap>


    <select id="selectTaskResultById" parameterType="com.huawei.qua.ebg.bo.samp.GetEbgSampTaskTodoDetailReqBo"
            resultType="com.huawei.qua.ebg.bo.samp.EbgSampTaskCategoryBo">
        SELECT
        t2.id,
        t1.busi_type,
        t1.check_type,
        t1.todo_id,
        t1.indicator,
        t2.ecare_rule_category,
        t2.rule_name,
        t2.quantity_result,
        t2.rejection_reason,
        t2.rejection_keyword,
        t1.quantity_result as result,
        t1.is_appeal as isAppealed,
        t1.is_check as isCheck,
        t1.appeal_confirm,
        t1.first_review_remark,
        t1.final_review_remark,
        t1.appeal_reason,
        <choose>
            <when test="busiType == 'eCare'">
                t1.ccr_assessor,t1.cse_assessor,t3.sr_num,t3.ccr_assessor as todoCcrAssessor,t3.cse_assessor as todoCseAssessor,
            </when>
            <otherwise>
                t1.pse_assessor,t1.cse_assessor,t4.sr_num,t4.pse_assessor as todoPseAssessor,t4.cse_assessor as todoCseAssessor,
            </otherwise>
        </choose>
        t1.create_user,
        t1.create_time,
        t1.is_cse,
        t1.is_ccr,
        t1.is_pse
        FROM
        t_ebg_samp_task_indicator_result t1
        left join t_ebg_samp_task_category_result t2 on t1.todo_id = t2.todo_id AND t1.indicator = t2.indicator
        <choose>
            <when test="busiType == 'eCare'">
                left join t_ebg_samp_task_todo t3 on t1.todo_id = t3.todo_id
            </when>
            <otherwise>
                left join t_ebg_samp_task_base t4 on t1.todo_id = t4.todo_id
            </otherwise>
        </choose>
        WHERE
        t1.todo_id = #{todoId}
        <if test="busiType != null and busiType != ''">
            and t1.busi_type = #{busiType}
        </if>
        <if test="checkType != null and checkType != ''">
            and t1.check_type = #{checkType}
        </if>
    </select>

    <!-- 查询记录 -->
    <select id="selectTaskResultList" parameterType="com.huawei.qua.ebg.bo.samp.EbgSampTaskCategoryResultBo" resultMap="EbgSampTaskResultBoResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM
        t_ebg_samp_task_category_result
        <where>
            <if test="todoId != null and todoId != ''">
                and todo_id = #{todoId}
            </if>
            <if test="busiType != null and busiType != ''">
                and busi_type = #{busiType}
            </if>
            <if test="checkType != null and checkType != ''">
                and check_type = #{checkType}
            </if>
            <if test="quantityResult != null and quantityResult != ''">
                and quantity_result = #{quantityResult}
            </if>
            <if test="indicator != null and indicator != ''">
                and indicator = #{indicator}
            </if>
            <if test="ruleName != null and ruleName != ''">
                and rule_name = #{ruleName}
            </if>
        </where>
    </select>

    <insert id="insertSampTodoLog" parameterType="com.huawei.qua.ebg.bo.task.SampTodoLogBo">
        INSERT INTO t_ebg_samp_task_todo_log (
            operator,
            operate_time,
            operate_type,
            operate_content,
            busi_type,
            check_type,
            todo_id,
            indicator
        ) VALUES (
                     #{operator},
                     now(),
                     #{operateType},
                     #{operateContent},
                     #{busiType},
                     #{checkType},
                     #{todoId},
                     #{indicator}
                 )
    </insert>


    <insert id="insertTodoAttach"  keyColumn="file_id" keyProperty="fileId"
        parameterType="com.huawei.qua.bo.inspection.FileBo" useGeneratedKeys="true">
        INSERT INTO  t_ebg_samp_task_todo_attachment(file_name,file_extension,uploader,upload_time,edm_doc_id,file_status)
        values (#{originFileName},#{fileExtName},#{creator},now(),#{edmDocId},#{status})
    </insert>


    <!-- 查询记录 -->
    <select id="getTaskResultById" parameterType="java.lang.Long" resultMap="EbgSampTaskResultBoResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM
        t_ebg_samp_task_category_result
        where id = #{id}
    </select>

    <!-- 更新记录 -->
    <sql id="Base_Column_List">
        id,
        busi_type,
        check_type,
        todo_id,
        indicator,
        ecare_rule_category,
        rule_name,
        quantity_result,
        rejection_reason,
        rejection_keyword,
        create_user,
        create_time
    </sql>

    <!-- 采样待办任务指标规则结果编辑 -->
    <update id="updateTodoRuleTaskResult" parameterType="com.huawei.qua.ebg.bo.task.TodoRuleTaskResultReqBo">
        UPDATE t_ebg_samp_task_category_result
        SET
        <if test="busiType != null and busiType != ''">
            busi_type = #{busiType},
        </if>
        <if test="checkType != null and checkType != ''">
            check_type = #{checkType},
        </if>
        <if test="todoId != null and todoId != ''">
            todo_id = #{todoId},
        </if>
        <if test="indicator != null and indicator != ''">
            indicator = #{indicator},
        </if>
        <if test="ecareRuleCategory != null and ecareRuleCategory != ''">
            ecare_rule_category = #{ecareRuleCategory},
        </if>
        <if test="ruleName != null and ruleName != ''">
            rule_name = #{ruleName},
        </if>
        <if test="rejectionReason != null and rejectionReason != ''">
            rejection_reason = #{rejectionReason},
        </if>
        <if test="rejectionKeyword != null and rejectionKeyword != ''">
            rejection_keyword = #{rejectionKeyword},
        </if>
        <if test="quantityResult != null and quantityResult != ''">
            quantity_result = #{quantityResult}
        </if>
        WHERE id = #{id}
    </update>

    <!-- 根据 todo_id 列表批量删除记录 -->
    <delete id="deleteByTodoIds">
        DELETE FROM t_ebg_samp_task_category_result
        WHERE todo_id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
```
